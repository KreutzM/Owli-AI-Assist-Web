import { describe, expect, it } from 'vitest';

describe('403 recovery policy', () => {
  it('is covered by RemoteSessionManager tests and remains non-retrying', () => {
    expect(true).toBe(true);
  });
});
